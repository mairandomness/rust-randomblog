{% extends "main" %}
{% block content %}

    <div class="container row padding-bottom">
        <h1>{{ post.title }}</h1>
        <div>
            <p>{{ post.content | safe }}</p>
        </div>
    </div>
    
{% endblock content %}